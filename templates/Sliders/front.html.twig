<section class="slider">
	<div class="flexslider">
	  	<ul class="slides">
	  		{% for slider in sliders %}
	    	<li>
	    		<a href="{{ path('slider_front_details', {id : slider.id}) }}"><img src="{{ asset('/uploads/sliders/') }}{{ slider.image }}" /></a>
    	    
    		</li>
    		{% endfor %}
	  	</ul>
	</div>
</section>