{% for categorie in sc2 %}
    <!-- section -->
    {% if(categorie.show_products == 1) %}
    {{ render(controller('App\\Controller\\Front\\FrontController::productInIndex', {id:categorie.id})) }}
    <!-- /section -->
    {% endif %}
    {% if(categorie.show_banners == 1) %}
        {{ render(controller('App\\Controller\\Back\\BannersController::bannersCategorie', {categorie:categorie.id})) }}
    {% endif %}
{% endfor %}

